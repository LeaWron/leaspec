# leaspec configuration
version: "1.1"

# Git 追踪设置
git:
  track_leaspec: true
  track_agent_dirs: false
  ignore_method: gitignore

# 项目元信息
project:
  name: "myspecskills"
  description: "客制化 spec skill — 融合 spec-kit, open-spec, superpowers"
